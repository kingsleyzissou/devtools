{
  "users": [
    {
      "name": "kingsley",
      "gecos": "AK",
      "ssh_pwauth": true,
      "ssh_authorized_keys": [],
      "sudo": "ALL=(ALL) NOPASSWD:ALL"
    },
    {
      "name": "root",
      "gecos": "AK",
      "ssh_pwauth": true,
      "ssh_authorized_keys": []
    }
  ],
  "chpasswd": {
    "list": "root:r\nkingsley:password42\n",
    "expire": false
  },
  "repo_update": true,
  "repo_upgrade": "all",
  "packages": [
    "go",
    "make",
    "rpm-build",
    "jq",
    "krb5-devel",
    "git",
    "podman",
    "osbuild-composer",
    "composer-cli",
    "cockpit",
    "nodejs",
    "npm",
    "openssl",
    "openscap-scanner",
    "scap-security-guide"
  ],
  "manage_resolv_conf": true,
  "resolv_conf": {
    "nameservers": [
      "10.38.5.26",
      "10.0.2.3"
    ]
  },
  "runcmd": [
    "sudo mkdir -p /home/kingsley/projects/osbuild",
    "cd /home/kingsley/projects/osbuild && git clone https://github.com/osbuild/osbuild-composer",
    "sudo mkdir -p /etc/osbuild-composer/repositories/",
    "sudo chown -R kingsley:kingsley /home/kingsley/projects",
    "sudo systemctl enable --now osbuild-composer.socket",
    "sudo systemctl enable --now cockpit.socket",
    "sudo cp -r /home/kingsley/projects/osbuild/osbuild-composer/test/data/repositories/. /etc/osbuild-composer/repositories/.",
    "sudo usermod -aG weldr,systemd-journal kingsley",
    "git config --system user.email testvm@osbuild.org",
    "git config --system user.name kingsley"
  ]
}
